apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend
  namespace: shop
spec:
  entryPoints:
    - web                       # Traefik's HTTP entrypoint (:80) on k3s
  routes:
    # match every path on any host — a reverse proxy fronts the cluster, so we don't match a Host()
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: frontend        # the ClusterIP Service from frontend-service.yaml
          port: 80
