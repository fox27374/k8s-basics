# Capstone: deploy a multi-tier app (optional)

Tie it all together: deploy a small frontend + backend + database app using the
objects from the previous chapters.

## Tasks
### 1. Deploy the database (Deployment/StatefulSet + PVC + Secret)
### 2. Deploy the backend (Deployment + Service + ConfigMap)
### 3. Deploy the frontend (Deployment + Service)
### 4. Expose the frontend via Ingress
### 5. Add probes and resource limits to each tier
