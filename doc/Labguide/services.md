# Services

Give Pods a stable address and load-balance across them; reach them by DNS.

## Commands
| Command | Description |
| --- | --- |
| kubectl expose | create a Service for a Deployment |
| kubectl get svc | list Services |
---

## Tasks
### 1. Expose a Deployment as a ClusterIP Service
### 2. Reach the Service by its DNS name from another Pod
### 3. Change the Service to NodePort
### 4. Access the app via <node-ip>:<nodePort>
